{
  "name": "terabox-resolver-saas",
  "version": "0.1.0",
  "private": true,
  "description": "Production-grade multi-provider share-link resolver SaaS for Telegram (TeraBox + extensible adapters).",
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20.11 <23"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "rimraf": "^6.0.1",
    "prettier": "^3.4.2"
  }
}
